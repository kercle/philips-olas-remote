# Library `olas`

High-level implementation of the fan control protocol and the `Controller` class which provides a convenient API for sending fan commands and tries to keep in sync with the fan state if the radio transmitter also logs for other remotes sending commands.
